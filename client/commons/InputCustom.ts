import {styled} from "@mui/styles";
import {InputBase} from "@mui/material";

export const InputCustomBase = styled(InputBase)({
    width: '100%',
    border: '1px solid #ced4da',
    borderRadius: '5px',
    backgroundColor: '#fff',
    lineHeight: '24px',
    padding: '6px 12px',
    color: '#495057'
})